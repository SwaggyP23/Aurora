#pragma once

#include <unordered_map>

namespace Aurora {

	// Have some predefined fonts loaded
	// have the ability to change the font in editor during runtime
	// the name of the font is stored in a container with the file path for its dependencies
	// Create an imgui panel for the fonts specifically or just called settings...

	class FontsLibrary
	{
	public:

	private:

	};

}