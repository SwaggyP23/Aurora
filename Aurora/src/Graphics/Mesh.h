#pragma once

// This is to be continued when reworking the shader to system to use Spir-V and Uniform Buffers that way meshes and materials are easier

#include "Buffer.h"
#include "VertexArray.h"

namespace Aurora {

	class Mesh
	{
	public:

	private:
		VertexArray m_VertexArray; // This will need to include a vertex buffer and an index buffer

	};

}